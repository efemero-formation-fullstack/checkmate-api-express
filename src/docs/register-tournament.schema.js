/**
 * @openapi
 * components:
 *   schemas:
 *     RegisterTournamentSchema:
 *       type: object
 *       required:
 *         - memberId
 *       properties:
 *         memberId:
 *           type: string
 *           format: uuid
 *           description: L'ID du membre à inscrire ou désinscrire.
 */
